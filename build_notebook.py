from pathlib import Path
import nbformat as nbf
R=Path(__file__).resolve().parent;n=nbf.v4.new_notebook();n.metadata={"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"}}
n.cells=[nbf.v4.new_markdown_cell("# Loan Default Risk with Business Cost Optimization\n\nPredict payment difficulty and minimize explicit decision costs."),nbf.v4.new_markdown_cell("## Method\n\nA stratified 60/20/20 split precedes preprocessing. Logistic Regression and CatBoost are compared. False negatives cost 10 units versus 1 for false positives; the threshold is chosen on validation data only."),nbf.v4.new_code_cell("from pathlib import Path\nimport sys,pandas as pd\nfrom IPython.display import Image,display\nROOT=Path.cwd();sys.path.insert(0,str(ROOT))\nfrom run_analysis import main"),nbf.v4.new_code_cell("results=main();display(pd.DataFrame({m:v['test'] for m,v in results['results'].items()}).T)"),nbf.v4.new_code_cell("display(Image(filename=str(ROOT/'outputs/figures/model_evaluation.png')));display(Image(filename=str(ROOT/'outputs/figures/feature_importance.png')))"),nbf.v4.new_markdown_cell("## Conclusion\n\nCatBoost leads on the untouched test set (ROC-AUC ≈ 0.764; average precision ≈ 0.251). Validation selects threshold 0.49 under the illustrative 10:1 cost ratio. Deployment requires exposure-aware costs, calibration, fairness testing, monitoring, and human governance.")]
nbf.write(n,R/'04_loan_default_cost_optimization.ipynb')
